import { items, itemsRelations } from './items';
import { locations, locationsRelations } from './locations';
import { locationsToLocations, locationsToLocationsRelations } from './locations-to-locations';
import { tags, tagsRelations } from './tags';
import { tagsToItems, tagsToItemsRelations } from './tags-to-items';
import { users, usersRelations } from './users';

export {
  items,
  itemsRelations,
  locations,
  locationsRelations,
  locationsToLocations,
  locationsToLocationsRelations,
  tags,
  tagsRelations,
  tagsToItems,
  tagsToItemsRelations,
  users,
  usersRelations
};
